# 体彩门店互动平台 性能优化设计

- 日期�?026-04-13
- 状态：已确认方向，待用户审阅书面规�?- 适用范围：React + Vite + Capacitor 前端、门店展示页、后台管理入口、与门店展示相关的数据拉取与轮询链路

## 1. 背景与问题定�?
当前项目存在以下共性问题：

1. 启动开发环境与页面加载负担偏重�?2. 浏览器端与安卓壳端都有明显卡顿、资源占用高、切页慢、发热等现象�?3. 项目前台展示、后台管理、心跳轮询、重视觉组件、大页面状态更新耦合较深，导致单点修改容易放大为整页重渲染�?4. 当前问题不是单一 bug，而是“持续运行负�?+ 首屏负担 + 页面结构过重 + 视觉效果成本过高”叠加造成的系统性性能问题�?
本次优化不采用零散修补策略，而采用“两阶段治理”路线：

- 阶段一：性能止血 —�?先显著降低运行时持续负担与交互卡顿�?- 阶段二：结构重构 —�?在止血基础上调整边界与职责，避免性能问题反复出现�?
## 2. 优化目标

### 2.1 业务目标

- 让门店展示端在浏览器和安卓设备上明显更流畅�?- 降低空闲状态下的无效资源占用�?- 缩短页面首次打开与路由切换等待感�?- 为后续继续扩展页面样式、后台功能和数据能力留出余量�?
### 2.2 技术目�?
- 降低前台页面持续 CPU、内存、网络占用�?- 降低首屏主包体积与非首屏代码加载量�?- 降低大页面状态变更引发的整页渲染�?- 为低性能安卓设备提供默认性能友好的渲染路径�?- 建立可重复验证的性能基线与验收方式�?
### 2.3 非目�?
本轮设计不包含以下内容：

- 不重做视觉风格�?- 不重写全部页面�?- 不引入大型状态管理框架，只为性能优化增加最小必要的数据层与边界调整�?- 不在没有证据的情况下大面积替换技术栈�?
## 3. 关键现状观察

基于当前代码，已确认的高风险点包括：

1. 路由入口同步引入大量页面，增加首屏与切页负担�?2. 门店布局层存在固定频率心跳请求，并且与页面路径变化直接耦合�?3. Home、PortalStyleSports 等页面体量大、状态多、视觉组件重，容易出现局部交互触发整页刷新�?4. 项目同时包含前端展示、后台管理、服务端与抓取相关依赖，说明开发负载和运行负载的边界不够清晰�?5. 兼容性与视觉效果策略倾向保守与复杂，可能对低性能终端造成额外负担�?
## 4. 方案决策

最终采用以下推荐路线：

**R1：运行时止血 �?页面拆分 �?路由懒加�?�?视觉降级 �?构建收敛**

选择该路线的原因�?
- 先解决“页面挂着不动也在持续消耗资源”的问题，收益最大�?- 再处理“页面本身太重”的问题，能显著改善交互卡顿�?- 然后处理“首屏和切页负担”，避免一开始把复杂度放在打包层面�?- 最后再收敛视觉与构建策略，减少误判和返工�?
未选方案：

- 纯微调方案：见效有限，无法根治�?- 直接全量重构：风险过大，周期过长，且缺少止血收益�?- 纯测量优先方案：适合作为治理体系，但不适合当前“已经普遍卡顿”的现状作为第一步�?
## 5. 两阶段设�?
### 5.1 阶段一：性能止血

目标：在不大规模改业务行为的前提下，快速降低持续占用、交互卡顿、首屏压力�?
包含四个工作流：

#### A. 心跳与数据更新治�?
优先级最高�?
设计要求�?
- 将展示端心跳从“页面各自触发”收敛为“布局层统一调度”�?- 页面不可见、应用切后台、或组件非活跃时暂停心跳�?- 将固�?10 秒轮询调整为可配置策略，并为低频场景使用更长间隔�?- 如果服务端返回的数据与上一次等价，则不重复写入 React state�?- 导航类命令增加去抖与幂等保护，避免重复跳转�?- 请求失败时不触发级联重试，不制造额外压力�?
预期结果�?
- 降低空闲 CPU 与网络占用�?- 降低安卓端发热与长时间挂屏卡顿�?- 为后续页面渲染优化创造稳定基础�?
#### B. 大页面拆分与渲染收敛

重点针对 Home、PortalStyleSports 及同类展示页�?
设计要求�?
- 将页面拆成“数据层容器 + 展示区块组件”�?- 高频变化区与低频静态区分离，避免局部状态变更触发整页刷新�?- 对静�?props 明确的组件使用稳定引用，减少重复计算与重复渲染�?- 将高成本视觉组件、轮播、公告、开奖、游戏入口等拆成独立边界�?- 页面保留原有功能，不在本轮增加额外业务能力�?
预期结果�?
- 减少点击、弹窗、抽屉、切�?tab 时的顿挫�?- 降低单次交互造成的渲染面积�?- 提升后续单模块单独优化的可行性�?
#### C. 路由与首屏减�?
设计要求�?
- 将后台管理页、不�?portal 风格页、工具页改为路由级懒加载�?- 首屏只保留当前门店首页必需代码�?- 页面间共享但不首屏必需的模块按需加载�?- 为懒加载页面提供轻量占位，不引入复杂 loading 动画�?
预期结果�?
- 缩短首次可交互时间�?- 降低主包大小与切页阻塞感�?
#### D. 视觉效果与静态资源降�?
设计要求�?
- 为低性能安卓设备提供默认性能模式�?- 高成本效果（大面积模糊、连续动画、复杂阴影、重 SVG 背景）必须可降级或关闭�?- 非首屏图片改为延迟加载�?- 远程图片与大资源避免阻塞首屏�?- 不影响核心功能可用性和品牌信息展示�?
预期结果�?
- 降低 GPU 压力与掉帧概率�?- 改善安卓设备滚动和页面停留体验�?
### 5.2 阶段二：结构重构

目标：从边界层面减少未来继续变慢的概率�?
包含三个方向�?
#### A. 运行壳层（Shell）收�?
职责�?
- 路由装配
- 懒加载边�?- 布局级上下文
- 错误边界与最轻量的全局状态承�?
要求�?
- App 与布局层只负责装配，不直接承载重业务逻辑�?- 业务数据请求与渲染细节移出壳层�?
#### B. 数据层集中管�?
职责�?
- 心跳
- 公告
- 门店配置
- 开奖与动态数�?- 轮询调度
- 页面可见性与缓存策略

要求�?
- 页面不各自直接实现轮询�?- 请求节奏、暂停恢复、去重写入在集中层管理�?- 后续新增页面时复用统一策略，而不是复制副本逻辑�?
#### C. 页面组装层与重特效层分离

职责�?
- 页面组装层：只负责组合业务区块�?- 重特效层：统一封装动画、毛玻璃、ticker、复杂背景等高成�?UI�?
要求�?
- 页面可以在“不改业务结构”的前提下切换普通模式与性能模式�?- 特效成本由集中位置控制，不散落在页面各处�?
## 6. 模块边界设计

### 6.1 Shell �?
包含：路由入口、门店布局、懒加载装配、基础上下文�?
原则�?
- 不放重计算�?- 不放业务区块拼装细节�?- 只保留页面生命周期与导航最小逻辑�?
### 6.2 Data �?
包含：心跳控制器、展示页数据聚合器、缓存与去重策略�?
原则�?
- 所有周期性任务都必须通过统一调度�?- 状态更新必须有“是否真的变化”的判断�?- �?UI 提供稳定、可复用的数据接口�?
### 6.3 Page Composition �?
包含：Home、PortalStyleSports 等页面容器及其业务区块拼装�?
原则�?
- 页面负责组装，不负责底层轮询与调度�?- 一个区块只解决一个清晰问题�?- 各区块支持独立测试与替换�?
### 6.4 Heavy UI / Effects �?
包含：轮播动画、ticker、背景特效、模糊玻璃组件、重阴影卡片等�?
原则�?
- 提供统一降级开关�?- 在低配设备默认使用轻量实现�?- 视觉收益不足以支撑性能成本的效果应删除，而非保留为默认行为�?
## 7. 数据流设�?
### 7.1 门店展示页数据流

推荐数据流：

1. Shell 层解�?`storeId` 与当前页面上下文�?2. Data 层接管心跳与展示数据请求�?3. Data 层对响应做去重、标准化、缓存与可见性控制�?4. 页面组装层只消费整理后的数据�?5. 区块组件只接收自己所需字段，避免整页透传大对象�?
### 7.2 页面交互数据�?
推荐规则�?
- 本地 UI 状态（弹窗、抽屉、tab）留在局部组件�?- 服务端数据留在集中数据层�?- 跨多个区块共享的数据从数据层向下分发，不做兄弟组件间隐式耦合�?
## 8. 错误处理策略

本次优化遵循“边界处理，不做过度防御”的原则�?
要求�?
- 网络请求失败时静默降级到已有数据或空状态，不触发高频重试�?- 心跳失败不应影响页面基础浏览能力�?- 懒加载失败提供最轻量可见错误提示，但不引入重型恢复机制�?- 性能模式切换失败时回退到普通展示，不阻断主流程�?
不做的事情：

- 不为理论上不会发生的内部状态增加额外复杂保护层�?- 不引入全局错误总线或大型状态机，仅为本次优化增加必要边界�?
## 9. 验证与测试设�?
### 9.1 验证原则

先建立基线，再逐项验证。每一项优化都必须能回答“具体减少了什么”�?
### 9.2 验证维度

1. 启动体验
   - 开发环境启动与热更新主观体�?   - 生产环境首页首次加载主观等待�?
2. 运行时负�?   - 空闲页面 CPU 占用趋势
   - 网络请求频率是否下降
   - 安卓设备长时间停留时发热与卡顿是否改�?
3. 交互流畅�?   - 首页滚动
   - 抽屉/弹窗打开关闭
   - tab 切换
   - 路由切换

4. 页面正确�?   - 心跳相关导航行为
   - 公告展示
   - 开�?资讯数据展示
   - 门店展示页与后台入口基本可用

### 9.3 验收方式

阶段一完成后，应至少满足以下结果：

- 页面空闲时的网络请求与状态更新次数明显减少�?- 门店主页面交互不再出现普遍性的明显顿挫�?- 安卓设备长时间展示时资源占用下降�?- 首屏与路由切换体感改善明显�?
阶段二完成后，应满足以下结果�?
- 前台展示新增功能不会再次轻易把页面拖回“大组件 + 高频重渲染”状态�?- 数据更新策略集中、清晰、可复用�?- 性能模式与普通模式边界清楚�?
## 10. 实施顺序

按以下顺序执行：

1. 心跳与数据更新治�?2. Home / PortalStyleSports 页面拆分与渲染收�?3. 路由级懒加载
4. 视觉效果分级与静态资源降�?5. 构建与兼容策略收�?6. 阶段二结构重�?
## 11. 风险与约�?
### 11.1 风险

- 心跳策略调整可能影响后台对门店在线状态或远程跳转的感知，需要保持现有业务行为不被破坏�?- 页面拆分过程中，如果边界划分不清，可能只是“把大文件拆成多个仍然耦合的小文件”�?- 视觉降级若没有统一策略，容易出现不同页面表现不一致�?
### 11.2 约束

- 本项目当前不�?git 仓库，无法执行“写入规格后直接提交 commit”的流程；规格文件仍将按约定路径写入，后续如接入 git 再纳入版本管理�?- 本次以现有技术栈为前提，不新增大型框架依赖作为默认方案�?
## 12. 最终决策摘�?
本项目的优化不从“构建参数微调”开始，也不直接进行全量重构�?
最终决策是�?
- 用两阶段路线推进�?- 阶段一先做运行时止血，优先消灭持续负担与大页面重渲染�?- 阶段二再做边界重构，确保性能不会因为后续迭代再次系统性恶化�?
该设计将作为后续实施计划的唯一依据