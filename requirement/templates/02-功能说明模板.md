# {项目名称} 功能说明

**文档版本**: 1.0  
**创建日期**: {YYYY-MM-DD}  
**最后更新**: {YYYY-MM-DD}  
**负责人**: {负责人姓名}

---

## 文档说明

本文档详细描述各功能模块的具体内容，包括功能清单、字段说明、处理逻辑、输入输出、异常处理等。

**适用读者**：产品经理、开发人员

---

## 1. 模块 A

### 1.1 功能描述

> 简要描述该模块的功能定位和业务价值。

- **功能定位**：{描述}
- **业务价值**：{描述}
- **使用频率**：{高/中/低}

### 1.2 功能依赖关系

**上游依赖**：
| 依赖模块 | 依赖内容 | 说明 |
|---------|---------|------|
| {模块 1} | {数据/接口} | {说明} |

**下游依赖**：
| 依赖模块 | 依赖内容 | 说明 |
|---------|---------|------|
| {模块 2} | {数据/接口} | {说明} |

### 1.3 功能优先级

| 优先级 | 功能名称 | 说明 |
|--------|---------|------|
| P0 | {功能 1} | 核心功能，必须实现 |
| P1 | {功能 2} | 重要功能，优先实现 |
| P2 | {功能 3} | 次要功能，后续迭代 |

**优先级说明**：
- **P0**：核心功能，影响系统可用性，必须实现
- **P1**：重要功能，影响用户体验，优先实现
- **P2**：次要功能，可在后续迭代中实现

### 1.4 功能清单

**菜单结构**：
```
{一级菜单} (parent_menu_id={id})
├── {二级菜单/模块}
│   ├── [菜单] {功能名称} (list)
│   ├── [按钮] 新增 (add)
│   ├── [按钮] 修改 (edit)
│   ├── [按钮] 删除 (remove)
│   ├── [按钮] 导出 (export)
│   └── [按钮] 导入 (import)
```

**功能列表**：
| 功能名称 | 操作类型 | 权限标识 | 菜单类型 | 父菜单 | 优先级 |
|---------|---------|---------|---------|--------|--------|
| {功能 1} | 查询 | {module}:{resource}:list | 菜单 | {父菜单} | P0 |
| {功能 1} | 新增 | {module}:{resource}:add | 按钮 | {功能 1} | P0 |
| {功能 1} | 修改 | {module}:{resource}:edit | 按钮 | {功能 1} | P0 |
| {功能 1} | 删除 | {module}:{resource}:remove | 按钮 | {功能 1} | P0 |
| {功能 1} | 导出 | {module}:{resource}:export | 按钮 | {功能 1} | P1 |

### 1.5 字段说明

| 字段 | 类型 | 必填 | 插入 | 编辑 | 列表 | 查询 | 查询方式 | 字典 | 表单类型 | 备注 |
|------|------|------|------|------|------|------|---------|------|---------|------|
| id | Long | - | × | × | √ | × | - | - | - | 主键 |
| name | String | √ | √ | √ | √ | √ | LIKE | - | input | {说明} |
| status | String | √ | √ | √ | √ | √ | EQ | sys_common_status | radio | 状态 |

**字段说明**：
- **插入**：对应 `isInsert`，是否为新增时的输入字段
- **编辑**：对应 `isEdit`，是否为修改时的输入字段
- **列表**：对应 `isList`，是否在列表页面显示
- **查询**：对应 `isQuery`，是否作为查询条件
- **查询方式**：对应 `queryType`，如 `EQ`（等于）、`LIKE`（模糊）、`BETWEEN`（范围）
- **字典**：对应 `dictType`，关联的系统字典类型
- **表单类型**：对应 `htmlType`，如 `input`、`select`、`radio`、`checkbox`、`textarea`、`datetime`、`switch`

### 1.6 输入输出说明

**输入数据来源**：
| 数据来源 | 类型 | 说明 |
|---------|------|------|
| 用户输入 | 表单 | 用户通过界面输入的数据 |
| 上游接口 | API | {接口名称} 返回的数据 |
| 数据库 | 表 | {表名} 中读取的数据 |

**输出数据格式**：
| 数据类型 | 格式 | 说明 |
|---------|------|------|
| 列表数据 | TableDataInfo | 分页数据，包含 total 和 rows |
| 单个对象 | AjaxResult + data | 单个实体对象 |
| 操作结果 | AjaxResult | 成功/失败标识和消息 |
| 导出文件 | Excel | 二进制文件流 |

### 1.7 处理逻辑

#### 1.7.1 业务规则

1. {规则 1：如"用户名不能重复"}
2. {规则 2：如"删除前需要检查关联数据"}
3. {规则 3：如"状态只能从 0 流转到 1"}

#### 1.7.2 特殊处理

- {描述需要在 Service 层补充的特殊逻辑}
- {如：数据脱敏处理}
- {如：批量操作的事务处理}

#### 1.7.3 异常处理

| 异常类型 | 触发条件 | 处理方式 | 错误提示 |
|---------|---------|---------|---------|
| 数据不存在 | 查询不存在的 ID | 抛出 ServiceException | "{记录}不存在" |
| 数据已存在 | 新增重复数据 | 抛出 ServiceException | "{记录}已存在" |
| 数据被引用 | 删除被关联的数据 | 抛出 ServiceException | "{记录}已被使用，无法删除" |
| 权限不足 | 无操作权限 | Spring Security 拦截 | "无权访问" |

### 1.8 自定义接口

| 接口路径 | 方法 | 说明 | 权限标识 | 输入参数 | 输出数据 |
|---------|------|------|---------|---------|---------|
| `/{module}/{resource}/custom` | POST | {说明} | {module}:{resource}:custom | {参数} | {响应} |

### 1.9 代码生成配置

**ruoyi-gen-cli 配置**：
```yaml
global:
  author: {作者}
  packageName: com.ruoyi.{module}
  moduleName: {module}
  tplCategory: {crud/tree/sub}
  tplWebType: element-plus
  parentMenuId: {菜单 ID}

tables:
  {table_name}:
    className: {ClassName}
    functionName: {功能名称}
    columns:
      # 列级配置
```

---

## 2. 模块 B

{复制模块 A 的结构，根据实际需求填写}

---

## 附录

### 附录 A：功能清单汇总

| 模块 | 功能名称 | 操作类型 | 权限标识 | 优先级 |
|------|---------|---------|---------|--------|
| 模块 A | {功能 1} | 查询 | {module}:list | P0 |
| 模块 A | {功能 1} | 新增 | {module}:add | P0 |
| 模块 B | {功能 2} | 查询 | {module}:list | P0 |

### 附录 B：字段类型参考

| 数据库类型 | Java 类型 | 前端类型 | 说明 |
|-----------|----------|---------|------|
| BIGINT | Long | Number | 主键、ID |
| VARCHAR(n) | String | String | 短文本 |
| TEXT | String | String | 长文本 |
| INT | Integer | Number | 整数 |
| DECIMAL(p,s) | BigDecimal | Number | 精确数值 |
| DATE | Date | String | 日期 |
| DATETIME | Date | String | 日期时间 |
| TINYINT(1) | Boolean | Boolean | 布尔值 |

### 附录 C：查询方式参考

| 查询方式 | 说明 | 适用场景 |
|---------|------|---------|
| EQ | 等于 | 精确匹配 |
| NE | 不等于 | 排除匹配 |
| GT | 大于 | 数值比较 |
| GTE | 大于等于 | 数值比较 |
| LT | 小于 | 数值比较 |
| LTE | 小于等于 | 数值比较 |
| LIKE | 模糊匹配 | 文本搜索 |
| BETWEEN | 范围查询 | 日期/数值范围 |

### 附录 D：表单类型参考

| 表单类型 | 说明 | 适用场景 |
|---------|------|---------|
| input | 单行文本框 | 短文本输入 |
| textarea | 多行文本框 | 长文本输入 |
| select | 下拉选择 | 字典项选择 |
| radio | 单选框 | 少量选项单选 |
| checkbox | 复选框 | 多选项多选 |
| datetime | 日期时间选择 | 日期时间输入 |
| switch | 开关 | 布尔值切换 |
| imageUpload | 图片上传 | 图片上传 |
| fileUpload | 文件上传 | 文件上传 |
| editor | 富文本编辑器 | 富文本内容 |

---

**文档结束**
